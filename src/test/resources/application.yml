spring:
  datasource:
    url: jdbc:h2:mem:company
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    #database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true

  profiles:
    active:
    - test
